/**
 * math operators are * / + - %
 */

const num1 = 5;
const num2 = 4;

const sum = num1 + num2;
console.log(sum);

const remain = num1 % num2;
console.log(remain);

/////////////////////////////////////////

/**
 * Math functions
 * using built-in Math class
 */

console.log('round = ', Math.round(5.14));

console.log('ceil = ', Math.ceil(5.14));

console.log('floor = ', Math.floor(5.14));

console.log('absolute value = ', Math.abs(-8));

/////////////////////////////////////////

/**
 * Math Properties / Constants
 * using built-in Math class
 */

console.log('Pi value is =', Math.PI);