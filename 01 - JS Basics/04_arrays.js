// define empty array
let arr = [];
// or non empty array
arr = [5, 4];

// add to array
arr.push(6);

console.log(arr);

// array indexing
console.log(arr[0]);

// array length
console.log(arr.length);